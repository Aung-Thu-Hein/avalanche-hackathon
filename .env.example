# Copy to .env and fill in.  NEVER commit .env.  NEVER use a wallet that holds real funds.
# Generate a throwaway key with:  cast wallet new
PRIVATE_KEY=0x...
FUJI_RPC_URL=https://api.avax-test.network/ext/bc/C/rpc

# Optional - only needed for `forge verify-contract` on Snowtrace/Routescan.
SNOWTRACE_API_KEY=
